#
# This file contains configuration flags to customize your site
#

# Name of your site (displayed in the header)
name: Srinath Obla

# Short bio or description (displayed in the header)
description: work = "science" if writing_down else "screwing_around"

# URL of your avatar or profile pic (you could use your GitHub profile pic)
avatar: https://raw.githubusercontent.com/srinathos/website-images/master/misc/profile_pic.jpg

#
# Flags below are optional
#

# Includes an icon in the footer for each username you enter
footer-links:
  dribbble:
  email: ossrinath@gmail.com
  facebook: 
  flickr:
  github: srinathos
  instagram: thespeedyos
  linkedin: srinathos
  pinterest:
  rss: # just type anything here for a working RSS icon
  twitter: thespeedyos
  stackoverflow: # your stackoverflow profile, e.g. "users/50476/bart-kiers"
  youtube: # channel/<your_long_string> or user/<user-name>
  googleplus: # anything in your profile username that comes after plus.google.com/


# Enter your Disqus shortname (not your username) to enable commenting on posts
# You can find your shortname on the Settings page of your Disqus account
disqus:

# Enter your Google Analytics web tracking code (e.g. UA-2110908-2) to activate tracking
google_analytics: UA-148594976-1

# Your website URL (e.g. http://barryclark.github.io or http://www.barryclark.co)
# Used for Sitemap.xml and your RSS feed
url: http://srinathos.github.io

# If you're hosting your site at a Project repository on GitHub pages
# (http://yourusername.github.io/repository-name)
# and NOT your User repository (http://yourusername.github.io)
# then add in the baseurl here, like this: "/repository-name"
baseurl: ""

#
# !! You don't need to change any of the configuration flags below !!
#

permalink: /:title/

# The release of Jekyll Now that you're using
version: v1.2.0
timezone: America/New_York
# Jekyll 3 now only supports Kramdown for Markdown
markdown: kramdown
kramdown:
  # Use GitHub flavored markdown, including triple backtick fenced code blocks
  input: GFM
  # Jekyll 3 and GitHub Pages now only support rouge for syntax highlighting
  syntax_highlighter: rouge
  syntax_highlighter_opts:
    # Use existing pygments syntax highlighting css
    css_class: 'highlight'

# Set the Sass partials directory, as we're using @imports
sass: { style: expanded } # You might prefer to minify using :compressed

#plugins
plugins:                       # modern key name
  - jekyll-sitemap
  - jekyll-feed
  - jekyll_picture_tag         # responsive <picture> tag
  - jekyll-paginate-v2         # optional nicer pagination

# ---------- Picture‑tag defaults ----------
picture:
  source: assets/images
  output: assets/generated
  presets:
    default:
      widths: [400, 800, 1200, 1600]
      formats: [avif, webp, jpg]
      sizes: "(min-width: 40em) 50vw, 100vw"

# ---------- Collections ----------
collections:
  albums:
    output: true
    permalink: /albums/:name/

# ---------- Defaults (optional) ----------
defaults:
  - scope: { path: "", type: posts }
    values:
      layout: post
      author: srinath
  - scope: { path: "", type: albums }
    values:
      layout: album

# Exclude these files from your production _site
exclude:
  - Gemfile
  - Gemfile.lock
  - LICENSE
  - README.md
  - CNAME
